#include <bits/stdc++.h>
using namespace std;

void ans(int t)
{
    int n;
    cin >> n;

    vector<string> phoneBook;
    for (int i = 1; i <= n; ++i)
    {
        string str;
        cin >> str;
        phoneBook.push_back(str);
    }

    string momNum;
    cin >> momNum;

    
}

int main()
{
    int t;
    cin >> t;

    for (int i = 1; i <= t; ++i)
    {
        ans(i);
    }

    return 0;
}